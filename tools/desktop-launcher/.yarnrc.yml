# node-modules linker (not PnP): Electron and electron-builder are only reliable
# with a real node_modules tree. PnP required a machine-specific unplugged
# electron.exe that broke launch on any other machine.
nodeLinker: node-modules
enableGlobalCache: false
