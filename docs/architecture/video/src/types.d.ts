// remotion-three extends JSX intrinsic elements (mesh, group, etc).
// We use the type definitions it ships with.
import "@remotion/three";
declare module "@react-three/fiber" {
  // placeholder so TS keeps these intrinsic types if a future
  // migration needs them.
}
